---
# NOTE: This is a template. DO NOT commit actual secrets to git.
# Use sealed-secrets, external-secrets, or vault for production.
apiVersion: v1
kind: Secret
metadata:
  name: lis-secrets
  namespace: lis-modern
type: Opaque
stringData:
  # Database
  POSTGRES_PASSWORD: "CHANGEME_IN_PRODUCTION"

  # JWT
  JWT_SECRET: "CHANGEME_MINIMUM_32_CHARACTERS_REQUIRED"

  # API Keys
  ENCRYPTION_KEY: "CHANGEME_32_CHARS_MINIMUM"

  # External APIs
  UIDAI_API_KEY: "CHANGEME"
  ABDM_CLIENT_SECRET: "CHANGEME"
  WHATSAPP_ACCESS_TOKEN: "CHANGEME"
  RAZORPAY_KEY_SECRET: "CHANGEME"

  # Monitoring
  SENTRY_DSN: "CHANGEME"
